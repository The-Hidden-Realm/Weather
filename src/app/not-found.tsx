import { ErrorPageContent } from "@/components/ErrorPageContent";

export default function NotFound() {
  return <ErrorPageContent title="Page not found" message="The page you're looking for doesn't exist." />;
}
